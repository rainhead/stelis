
# Racket bytecode (regenerated by raco/DrRacket)
compiled/

# Stelis derived state (build cache; safe to delete — forces a full rebuild)
.stelis/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
# Zero-byte flock the beads smart gate (BD_SMART_GATE migrations) creates at
# repo root and never unlinks — per-checkout runtime state.
.beads.gate.lock
